<UserControl x:Class="Burger_Customiser.UserControls.OrderListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Burger_Customiser.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="800">
    <Grid>
        <Rectangle Fill="#242424" RadiusX="20" RadiusY="20" Grid.ColumnSpan="2" />
        <TextBlock x:Name="TxtName" Text="Produktname" Margin="20,0,0,0" Style="{StaticResource Header2}" Height="24" Width="300" HorizontalAlignment="Left" />

        <WrapPanel ScrollViewer.VerticalScrollBarVisibility="Disabled" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0">
            <TextBlock x:Name="TxtPrice" Text="0,69 €" Style="{StaticResource Header2}" HorizontalAlignment="Right" Height="24" />
            <Button Content="-" Style="{StaticResource Button1}" />
            <TextBlock x:Name="TxtAmount" Text="0" Style="{StaticResource Header2}" />
            <Button Content="+" Style="{StaticResource Button1}" />
        </WrapPanel>
    </Grid>
</UserControl>
<UserControl x:Class="Burger_Customiser.UserControls.NavigationFooter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15" />
            <RowDefinition />
            <RowDefinition Height="15" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Content="ZURÜCK" Grid.Column="0" Grid.Row="1" Style="{StaticResource CornerButton}" Click="Button_Back_Click" />
        <Button Content="WEITER" Grid.Column="2" Grid.Row="1" Style="{StaticResource CornerButton}" Click="Button_Next_Click" />
    </Grid>
</UserControl>